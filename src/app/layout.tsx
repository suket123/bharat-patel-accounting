import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bharat Patel | Professional Canadian Tax & Accounting Services",
  description: "30+ years of accounting experience. Trusted Personal (T1) and Corporate (T2) tax preparation in Scarborough, Ontario, Canada.",
  keywords: ["Accounting", "Tax Return", "Canada", "Scarborough", "Personal Tax", "Corporate Tax", "T1", "T2", "Bharat Patel"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
